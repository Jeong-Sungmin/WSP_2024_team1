{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "folks": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
    // 기타 경로에 대한 규칙 설정
  }
}
